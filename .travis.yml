sudo: required

language: generic

services:
    - docker

before_install:
    - docker build -t anasso/docker-react -f Dockerfile.dev .

#script: 
#    - docker run anasso/docker-react-ci-aws npm run test -- --coverage